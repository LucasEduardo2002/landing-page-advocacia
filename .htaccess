RewriteEngine On

# Canonical único: sempre HTTPS + WWW
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^www\.andregustavoadvocacia\.com\.br$ [NC]
RewriteRule ^ https://www.andregustavoadvocacia.com.br%{REQUEST_URI} [R=301,L,NE]
